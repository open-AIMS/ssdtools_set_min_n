# Project: <repo-name>

This file provides **project-specific context only** — repo structure, dependencies,
analysis decisions, and prompt logging. It does not impose personal style preferences
on collaborators.

**For Claude:** If a `CLAUDE.md` exists in the parent directory of this repo,
read it before proceeding — it may contain user-specific environment and style
preferences. If no such file exists, apply sensible R defaults and ask if
anything is unclear. This repo-level file takes precedence over any parent-level
file where they conflict.

---

## 1. Repo Type

<!-- Delete whichever does not apply -->

**R package** — standard usethis/devtools structure, R CMD check via GitHub
Actions, pkgdown site, testthat, roxygen2.

**Research compendium** — package-like structure, Quarto for reporting,
Bayesian analysis via brms / Stan. No GitHub Actions.

**Analysis project** — scripts and Quarto documents, no package structure.

---

## 2. What This Repo Does

<!-- 2–4 sentences describing the purpose of this package or analysis.
     What problem does it solve? What are the key inputs and outputs? -->

---

## 3. Package Dependencies in Scope

<!-- Delete whichever does not apply -->

**For DESCRIPTION-based repos:** Dependencies are defined in `DESCRIPTION`
(Imports + Suggests). Read that file to determine what is available.

**For analysis projects:** See `packages.R` (or `libraries.R`) at the project
root for the canonical list of packages in use.

<!-- Note any packages that are non-obvious or installed from GitHub rather
     than CRAN, e.g.:
     - `bayesnec` — installed from GitHub (open-AIMS/bayesnec), not CRAN
-->

---

## 4. Key Files and Structure

<!-- Point Claude to the most important files so it doesn't have to explore
     blindly. Delete or extend as appropriate for repo type. -->

### R package layout
```
R/              # exported functions — one file per logical group
tests/testthat/ # testthat test files (test-<name>.R)
man/            # auto-generated by roxygen2, do not edit manually
data-raw/       # scripts that produce data objects in data/
vignettes/      # long-form documentation
DESCRIPTION     # package metadata, imports, suggests
NEWS.md         # changelog (one entry per version)
```

### Research compendium / analysis layout
```
R/              # helper functions sourced by Quarto docs
data/           # raw input data (read-only)
data-derived/   # processed data produced by scripts
analysis/       # Quarto documents (.qmd)
outputs/        # figures, tables, model objects
DESCRIPTION     # (if present) declares dependencies
```

---

## 5. Analysis Context

<!-- For research compendia and analysis projects only.
     Describe the statistical approach, key model structure, or domain context
     that Claude needs to give sensible suggestions. For example:
     - Data type and structure (e.g. concentration-response, time series)
     - Modelling framework (e.g. brms, Stan, frequentist via drc)
     - Any domain-specific conventions (e.g. log-scale concentrations,
       specific distributional assumptions)
     Delete this section for pure R packages. -->

---

## 6. Known Constraints or Decisions

<!-- Document any deliberate choices Claude should not second-guess, e.g.:
     - "We use a log-logistic model here — do not suggest alternatives
       without being asked."
     - "This function intentionally avoids dplyr to keep the dependency
       footprint small."
     - "HPC runs must use only base R and packages available in the .sif"
     Leave blank or delete if none. -->

---

## 7. Prompt Log

Session logs for this project are in `prompts/`. Create the folder if it does
not exist. Use a short kebab-case descriptor as the filename for each session
(e.g. `bayesian-model-refactor.md`). If a file with that name already exists,
append to it.

Log format for each session entry:

```
## Session: <descriptor>
Date: <YYYY-MM-DD>
Model: <model name and version>

### Prompts and Responses

**User:** <prompt text>

**Claude:** <summary of response — full code blocks where relevant, prose summarised>

---
```
